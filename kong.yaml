# 📁 Cấu trúc repo Git mẫu:
#
# kong-config-repo/
# ├── kong.yaml          <-- File cấu hình DB-less
# ├── .github/
# │   └── workflows/
# │       └── kong-config.yaml <-- CI/CD deploy lên K3s

# --- kong.yaml ---
_format_version: "2.1"

services:
  - name: basket-api
    url: http://basket-api.default.svc.cluster.local
    routes:
      - name: basket-route
        paths:
          - /basket